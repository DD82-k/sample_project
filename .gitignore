# === 编译产物 — 禁止提交 ===
build/
*.bin
*.elf
*.map
*.hex
flash_args
compile_commands.json
CMakeCache.txt
CMakeFiles/
*.cmake

# SDK 配置 — 每台机器不同，用 sdkconfig.defaults 共享默认值
sdkconfig
sdkconfig.old

# 自动下载的组件 — idf.py 会自动拉取
managed_components/

# IDE / 编辑器
.vscode/
.idea/
*.swp
*.swo

# 操作系统
Thumbs.db
.DS_Store

# === 以下必须保留追踪 ===
# 源码：*.c *.h
# 字体：ui/fonts/*.c
# UI：ui/*.c ui/*.h
# 组件：**/CMakeLists.txt
# 锁定：dependencies.lock（版本锁定）
# 默认配置：sdkconfig.defaults
